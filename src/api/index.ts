import sso from './sso';
import user from './user';
import auth from './auth';
import courses from './courses';
import network from './network';
import certificates from './certificates';

const api = { sso, user, auth, courses, network, certificates };

export default api;
